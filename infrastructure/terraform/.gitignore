# Terraform state
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfstate.backup

# Backend config — generated by 00-bootstrap-tf-backend.sh, contains bucket names
backend.hcl

# Variable values — contains secrets (db_password, etc.)
terraform.tfvars

# Crash logs
crash.log
crash.*.log

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI history
.terraform.tfvars
