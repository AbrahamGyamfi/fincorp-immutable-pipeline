# GHSA-3xgq-45jj-v275 / CVE-2024-21538 — cross-spawn ReDoS
# Location: usr/local/lib/node_modules/npm/node_modules/cross-spawn
# This package is bundled inside npm (the package manager) which ships inside
# node:20-alpine. npm is not exposed as a network service and is not in the
# application's runtime path. The app runs as non-root user 'fincorp' with
# no npm access. Risk: none to deployed application.
GHSA-3xgq-45jj-v275
CVE-2024-21538
