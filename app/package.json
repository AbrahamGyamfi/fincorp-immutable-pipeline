{
  "name": "fincorp-control-plane",
  "version": "1.0.0",
  "description": "FinCorp Pipeline Control Center — full-stack dashboard",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "jest --coverage --forceExit",
    "build:client": "cd client && npm ci && npm run build"
  },
  "dependencies": {
    "@aws-sdk/client-backup": "^3.1073.0",
    "@aws-sdk/client-ecr": "^3.1073.0",
    "@aws-sdk/client-rds": "^3.1073.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.2",
    "supertest": "^6.3.4"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
